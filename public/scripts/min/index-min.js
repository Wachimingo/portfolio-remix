const childDivs = document.querySelectorAll('[class*="menu-child-div"]'), x = window.matchMedia("(min-width: 768px)"), signupLink = document.getElementById("signup"), login_logoutLink = document.getElementById("login_logout"), media = document.currentScript.getAttribute("media"); "none" === media && (window.onload = () => { const e = document.styleSheets, n = e.length; let t = 0; for (; t < n; t++)e[t].media.mediaText = "all" }); const getCookie = e => { let n = e + "=", t = decodeURIComponent(document.cookie).split(";"); for (let e = 0; e < t.length; e++) { let i = t[e]; for (; " " == i.charAt(0);)i = i.substring(1); if (0 == i.indexOf(n)) return i.substring(n.length, i.length) } }; getCookie("token") ? (signupLink.remove(), login_logoutLink.href = "/auth/logout", login_logoutLink.innerText = "Log Out") : (login_logoutLink.href = "/auth/signin", login_logoutLink.innerText = "Sign In"), x.matches ? childDivs.forEach((e => { e.classList.remove("navbar-item-none") })) : childDivs.forEach((e => { e.classList.add("navbar-item-none") })); const resizedWindow = e => { e.matches ? childDivs.forEach((e => { e.classList.remove("navbar-item-none") })) : childDivs.forEach((e => { e.classList.add("navbar-item-none") })) }, openMenu = () => { childDivs.forEach((e => { e.classList.toggle("navbar-item-none") })) }; x.addEventListener("change", resizedWindow); const hamBtn = document.getElementById("hamburger-menu"); hamBtn.addEventListener("click", openMenu);